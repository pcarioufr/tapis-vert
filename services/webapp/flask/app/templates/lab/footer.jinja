<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Footer with Multiple Buttons and Sliding Panel</title>
    <style>
        /* Reset margin and padding */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        /* Basic body styling */
        body {
            font-family: Arial, sans-serif;
            height: 100vh;
            position: relative;
            overflow-x: hidden;
        }

        /* Footer styling */
        footer {
            position: fixed;
            bottom: 0;
            left: 0;
            width: 100%;
            height: 60px;
            background-color: #333;
            color: white;
            display: flex;
            justify-content: center;
            align-items: center;
            z-index: 10;
        }

        footer button {
            background-color: #f0ad4e;
            color: white;
            border: none;
            padding: 10px 20px;
            cursor: pointer;
            margin: 0 10px; /* Add space between buttons */
        }

        /* Sliding side panel styling */
        .container-panel {
            position: absolute;
            bottom: -200px; /* Start below the footer */
            width: 100px; /* Only 100px wide */
            height: 200px;
            background-color: #f1f1f1;
            box-shadow: 0px -2px 5px rgba(0, 0, 0, 0.2);
            transition: transform 0.3s ease, left 0.3s ease; /* Smooth slide-in effect and position transition */
            z-index: 5; /* Ensure it appears below the footer */
        }

        /* Slide the panel up (visible) */
        .container-panel.show {
            transform: translateY(-100%);
        }
    </style>
</head>
<body>

    <footer>
        <button onclick="togglePanel(event)">Button 1</button>
        <button onclick="togglePanel(event)">Button 2</button>
    </footer>

    <div class="container-panel" id="panel">
        <div style="padding: 20px;">
            <h3>Floating Panel Content</h3>
            <p>This is some dummy content inside the floating panel. It should slide in from below the footer.</p>
        </div>
    </div>

    <script>
        function togglePanel(event) {
            const panel = document.getElementById('panel');
            
            // Get the clicked button's position
            const buttonRect = event.target.getBoundingClientRect();

            // Set the panel's left position to align with the button
            panel.style.left = `${buttonRect.left}px`;

            // Toggle the visibility of the panel
            panel.classList.toggle('show');
        }
    </script>

</body>
</html>
